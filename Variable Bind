1 - SET SERVEROUTPUT ON 
VARIABLE NOMBRE VARCHAR2(20);

DECLARE 
    v_salario NUMBER;
BEGIN
    SELECT FIRST_NAME, SALARY
    INTO :NOMBRE , v_salario
    FROM EMPLOYEES 
    WHERE EMPLOYEE_ID = 100;

    DBMS_OUTPUT.PUT_LINE(:NOMBRE|| '  ' || v_salario);
END;
